CREATE TABLE telegrambotshelter.users_contact_info
(
id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
chat_id bigint NOT NULL,
name varchar NOT NULL,
surname varchar NOT NULL,
phone bigint UNIQUE NOT NULL,
PRIMARY KEY (id));